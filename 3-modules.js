// Modules - Encapsulated Code (only share minimum)
// CommonJS, every file is module (by default)

const names = require('./3-modules2');
const sayHi = require('./3-modules3');
const data = require('./3-modules4');
require('./3-modules5');

// sayHi('susan');
// sayHi(names.john);
// sayHi(names.peter);
